/** @type {import('jest').Config} */
import defaults from "jest-config";
const config = {
    verbose: true,
    testTimeout: 10000,
    transform: {}
  };
  

//export default config;
export default config

