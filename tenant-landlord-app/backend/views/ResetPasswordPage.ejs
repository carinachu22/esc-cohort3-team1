<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Reset Password</title>
        <style>
            /* CSS to center the content */
            body {
                display: flex;
                justify-content: center;
                align-items: center;
                height: 100vh; /* This will make the container center vertically */
            }
            .container {
                text-align: center;
            }
            form {
                display: inline-block; /* This will prevent the form from taking full width */
                text-align: center;
            }
            /* CSS to increase input width */
            input[type="password"] {
                width: 300px; /* You can adjust this value to your preference */
            }
            input[type="submit"] {
                width: 120px; /* You can adjust this value to your preference */
            }
        </style>
        <script>
            var status='<%- JSON.stringify(status) %>';
            if(JSON.parse(status) == "verified"){
                if (confirm("proceed to login")){
                    //window.location object can be used to get current page address(URL) and redirect the browser to a new page
                    window.location.href = "http://localhost:3000/";
                }
            }
        </script>
    </head>
    <body >
        <div class="container">
            <h1 >Email: <%= email%></h1>
            <form action="" method="post">
                <input type="password" name="password" id="password" placeholder="new password" />
                <br/>
                <br/>
                <input type="submit" value="submit" />
            </form>
        </div>

    </body>
</html>